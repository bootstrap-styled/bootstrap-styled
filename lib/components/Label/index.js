import styled from 'styled-components';

var Label = styled.label.withConfig({
    displayName: 'Label__Label',
    componentId: 'Label__Label-8xm4om'
})(['/* Reboot Scss */touch-action: manipulation;/* Allow labels to use \'margin for spacing. */display: inline-block;margin-bottom: .5rem;']);

export default Label;