/**
 * A Table Row with natures
 */
import styled from 'styled-components';
import { getBackgroundUtilities } from '../../styled/utilities/background';

var Tr = styled.tr.withConfig({
  displayName: 'Tr__Tr',
  componentId: 'Tr__Tr-1r01it6'
})(['', ''], getBackgroundUtilities());

export default Tr;