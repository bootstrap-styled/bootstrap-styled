The `<Samp />` component is a phrase tag. It defines sample output from a computer program. 
This component has no props.

`<Samp />` component example:

```js
<Samp>Sample output from a computer program</Samp>
```
