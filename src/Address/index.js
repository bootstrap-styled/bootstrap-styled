/**
 * Address component
 */

import styled from 'styled-components';

const Address = styled.address`
  /* Reboot Scss */
  margin-bottom: 1rem;
  font-style: normal;
  line-height: inherit;
`;

export default Address;
