/* A Legend (Box) */

import styled from 'styled-components';

const Legend = styled.legend`
  display: block;
  width: 100%;
  padding: 0;
  margin-bottom: .5rem;
  font-size: 1.5rem;
  line-height: inherit;
`;

/** @component */
export default Legend;
