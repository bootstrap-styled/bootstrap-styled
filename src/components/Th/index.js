/**
 * A Table Header Cell
 */
import styled from 'styled-components';

const Th = styled.th`
  /* Reboot Scss*/
  /* Centered by default, but left-align-ed to match the 'td's below. */
  text-align: left;
`;

export default Th;
